# Docker Service Failed to Start — Root Cause and Fix

## Problem

Docker service failed to start with:

```text
failed to load listeners: no sockets found via socket activation:
make sure the service was started by systemd
```

Systemd logs showed:

```text
docker.service: Main process exited, code=exited, status=1/FAILURE
```

---

# Root Cause

Docker was configured to use systemd socket activation:

```text
-H fd://
```

This means:

* `dockerd` expects systemd to provide an already-open socket
* the socket is managed by `docker.socket`
* if `docker.socket` is missing, disabled, or not running,
  Docker cannot create its listener
* therefore the daemon exits immediately

The failure was not caused by:

* containers
* images
* storage corruption
* networking
* disk space

It was specifically a broken or inactive `docker.socket`.

---

# How Docker Socket Activation Works

Systemd starts:

```text
docker.socket
```

which creates:

```text
/run/docker.sock
```

Then systemd launches:

```text
docker.service
```

and passes the socket file descriptor to `dockerd`.

If the socket is unavailable, Docker prints:

```text
failed to load listeners: no sockets found via socket activation
```

and exits with status code `1`.

---

# Fix

## Restart socket and service

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker.socket
sudo systemctl restart docker.service
```

---

## Enable socket permanently

```bash
sudo systemctl enable docker.socket
sudo systemctl start docker.socket
sudo systemctl start docker
```

---

# Verification

Check service status:

```bash
sudo systemctl status docker
sudo systemctl status docker.socket
```

Check Docker functionality:

```bash
docker ps
```

---

# Expected Working Configuration

The Docker service should contain:

```ini
ExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock
```

The Docker socket should contain something similar to:

```ini
[Socket]
ListenStream=/run/docker.sock
SocketMode=0660
SocketUser=root
SocketGroup=docker
```

---

# Summary

Docker failed because `dockerd` was configured to receive its socket from systemd, but `docker.socket` was unavailable.

Restarting/enabling `docker.socket` restored the socket activation chain and allowed Docker to start normally.





```````

Exact problem line causing your crash:

❌ THE ROOT CAUSE (confirmed)

In your Dockerfile:

RUN echo "source /home/user/franka-docker/franka_ws/install/setup.bash" >> ~/.bashrc
💥 Why this breaks everything

At Docker build/run time, this line forces:

container starts
→ .bashrc runs automatically
→ tries to source setup.bash
→ BUT install/ does not exist yet
→ CRASH

That is exactly your error:

No such file or directory: install/setup.bash
🧠 Important insight

You already proved:

install/setup.bash EXISTS on host ✔

But Docker is:

👉 expecting it at startup BEFORE anything is built or verified

🔧 THE CORRECT FIX
❌ REMOVE this line completely:
source /home/user/franka-docker/franka_ws/install/setup.bash
✅ Replace with SAFE version:
Option 1 (BEST practice):

Do NOT auto-source anything:

# workspace will be sourced manually after colcon build
Option 2 (if you REALLY want auto-source)

Only if you're 100% sure build always happens:

RUN echo "if [ -f /home/user/franka-docker/franka_ws/install/setup.bash ]; then source /home/user/franka-docker/franka_ws/install/setup.bash; fi" >> ~/.bashrc

This prevents crash.

🚨 WHY YOUR CURRENT SETUP IS BAD

You are doing:

Docker starts
→ expects ROS workspace already built
→ but build is not guaranteed
→ crash
🧠 Correct ROS2 + Docker logic

Proper flow should be:

1. start container
2. build workspace (colcon build)
3. THEN source install/setup.bash

NOT reverse.

🔥 ALSO IMPORTANT (you have another hidden risk)

This line:

source /opt/ros/$ROS_DISTRO/setup.bash

is OK ✔

BUT ONLY if:

ROS_DISTRO = jazzy

Make sure this is correct:

echo $ROS_DISTRO
🚀 FINAL FIX SUMMARY
You MUST change:
# REMOVE THIS
source /home/user/franka-docker/franka_ws/install/setup.bash
Keep:
ROS Jazzy setup ✔
OpenRobot env ✔
PATH / LD_LIBRARY_PATH ✔
🧠 Why this solves EVERYTHING

After removing that line:

Docker starts clean ✔
no missing setup.bash crash ✔
you manually control ROS environment ✔

``````````
















